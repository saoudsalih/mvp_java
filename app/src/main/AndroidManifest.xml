<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.aikya.mt">


    <application
        android:name=".app.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activity.viewImpl.ProfileActivity"></activity>
        <activity android:name=".view.activity.viewImpl.OtpValidateActivity" />
        <activity android:name=".view.activity.viewImpl.RegistrationActivity" />
        <activity android:name=".view.activity.viewImpl.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.activity.viewImpl.DashboardActivity"
            android:theme="@style/ActionBar" />
    </application>

</manifest>